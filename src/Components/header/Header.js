import React from "react";
import HeaderList from "./headerList/HeaderList";

const Header = () => {
  return (
    <header>
      <nav>
        <HeaderList />
      </nav>
    </header>
  );
};

export default Header;
